{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/advanced-complex-analysis-mittag-leffler/schema.json",
  "title": "advanced-complex-analysis-mittag-leffler widget params",
  "description": "Mittag-Leffler explorer (the meromorphic dual of Weierstrass factorization). The reader picks a meromorphic function with a known principal-part expansion and steps the number of pole-terms N; the widget plots the partial sum along the real axis against the target, so the prescribed poles appear one pair at a time and the sum converges. π cot(πz) = 1/z + Σ 2z/(z²−n²) (simple poles at the integers) and π²/sin²(πz) = Σ 1/(z−n)² (double poles at the integers) are the classic examples. The partial-sum eval is intrinsic (a `kind` enum); params carry the case menu.",
  "meta": {
    "family": "advanced-complex-analysis-mittag-leffler",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title", "functions"],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id; the script derives -sel/-n/-svg/-out." },
    "title": { "type": "string", "description": "Display title rendered inside .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional short hint rendered inside .hd > .hint." },
    "functions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 4,
      "description": "Each meromorphic function with a Mittag-Leffler expansion offered in the dropdown.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "kind"],
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$", "description": "Stable option value." },
          "label": { "type": "string", "description": "Plain-text dropdown label, e.g. 'π cot(πz)'." },
          "kind": {
            "type": "string",
            "enum": ["cot", "csc2"],
            "description": "cot = π cot(πz) = 1/z + Σ 2z/(z²−n²); csc2 = π²/sin²(πz) = Σ 1/(z−n)²."
          },
          "note": { "type": "string", "maxLength": 120, "description": "Optional plain-text remark about this expansion." }
        }
      }
    }
  }
}
