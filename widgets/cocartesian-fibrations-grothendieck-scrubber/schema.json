{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/cocartesian-fibrations-grothendieck-scrubber/schema.json",
  "title": "cocartesian-fibrations-grothendieck-scrubber widget params",
  "description": "Proof-scrubber walkthrough of the Grothendieck construction $\\int F$. Used in cocartesian-fibrations.html §5.",
  "meta": {
    "family": "cocartesian-fibrations-grothendieck-scrubber",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "bodyMarkup",
    "bodyScript"
  ],
  "properties": {
    "widgetId": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "DOM id for the widget host element. Must match the id baked into bodyMarkup."
    },
    "bodyMarkup": {
      "type": "string",
      "description": "ARTIFACT. Verbatim widget host bytes (the inline `<div class=\"widget\" id=\"...\"></div>`). Carried through unchanged so the round-trip stays byte-identical."
    },
    "bodyScript": {
      "type": "string",
      "description": "ARTIFACT. Verbatim driving script bytes including the leading newline + `<script>` tag and trailing `</script>`. Carries the IIFE that wires up the widget; preserved byte-for-byte."
    }
  }
}
