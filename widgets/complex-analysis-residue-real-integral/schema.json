{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/complex-analysis-residue-real-integral/schema.json",
  "title": "complex-analysis-residue-real-integral widget params",
  "description": "Real integrals by residues. The reader picks an integrand; the widget draws the upper-half-plane semicircular contour (real axis + large arc, whose contribution → 0), plots the integrand's poles (those in the upper half-plane are enclosed and highlighted), and computes ∫_{-∞}^∞ f dx = 2πi · Σ Res over the enclosed poles, matching the closed form. The 2πi·Σ arithmetic is intrinsic; params carry each integrand's poles, residues, and answer.",
  "meta": {
    "family": "complex-analysis-residue-real-integral",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title", "integrands"],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id; the script derives -sel/-svg/-out." },
    "title": { "type": "string", "description": "Display title rendered inside .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional short hint rendered inside .hd > .hint." },
    "integrands": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "description": "The integrands offered in the dropdown.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "poles", "result"],
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$", "description": "Stable option value." },
          "label": { "type": "string", "description": "Plain-text dropdown label, e.g. '1/(x²+1)'." },
          "result": { "type": "string", "maxLength": 32, "description": "Closed-form value of the real integral, e.g. 'π' or 'π/√2 ≈ 2.221'." },
          "note": { "type": "string", "description": "Optional one-line remark shown in the readout." },
          "poles": {
            "type": "array",
            "minItems": 1,
            "maxItems": 8,
            "description": "All poles of the integrand. The widget encloses those with im > 0.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["re", "im", "resRe", "resIm"],
              "properties": {
                "re": { "type": "number", "description": "Real part of the pole." },
                "im": { "type": "number", "description": "Imaginary part (im > 0 ⇒ enclosed by the UHP contour)." },
                "resRe": { "type": "number", "description": "Real part of the residue there." },
                "resIm": { "type": "number", "description": "Imaginary part of the residue there." },
                "label": { "type": "string", "maxLength": 16, "description": "Optional plain-text pole label, e.g. 'i'." }
              }
            }
          }
        }
      }
    }
  }
}
