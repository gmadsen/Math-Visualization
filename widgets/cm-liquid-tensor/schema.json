{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/cm-liquid-tensor/schema.json",
  "title": "cm-liquid-tensor widget params",
  "description": "Bespoke verbatim-renderer slug for the Classical projective vs liquid tensor product (parameterised) widget on the condensed-mathematics topic. Carries opaque bodyMarkup + bodyScript strings.",
  "meta": {
    "family": "verbatim",
    "dimension": "2d",
    "gesture": "click",
    "role": "interactive"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "title",
    "hint",
    "bodyMarkup",
    "bodyScript"
  ],
  "properties": {
    "widgetId": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "DOM id for the widget root."
    },
    "title": {
      "type": "string",
      "description": "Header title (rendered inside .hd > .ttl)."
    },
    "hint": {
      "type": "string",
      "description": "Header hint (rendered inside .hd > .hint)."
    },
    "bodyMarkup": {
      "type": "string",
      "description": "Verbatim markup for the widget \u2014 full widget div block.",
      "x-artifact": true
    },
    "bodyScript": {
      "type": "string",
      "description": "Verbatim driving script tag.",
      "x-artifact": true
    }
  }
}