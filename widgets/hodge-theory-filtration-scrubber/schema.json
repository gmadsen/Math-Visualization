{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/hodge-theory-filtration-scrubber/schema.json",
  "title": "hodge-theory-filtration-scrubber widget params",
  "description": "Scrubber for the Hodge filtration F^p H^n = ⊕_{p'≥p} H^{p',n-p'}. The reader picks a variety and a cohomological degree n, then scrubs the threshold p; the summands H^{p',q} of H^n split into the part inside F^p (p'≥p) and the complementary part, which is the conjugate of F^{n-p+1}, dramatising the recovery relation H^n = F^p ⊕ conj(F^{n-p+1}). Data-driven: each variety carries a square Hodge matrix h[p][q]; the complex dimension is derived as h.length-1.",
  "meta": {
    "family": "hodge-theory-filtration-scrubber",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title", "varieties"],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id for the outer <div class=\"widget\">; the script derives -sel/-deg/-p/-svg/-out." },
    "title": { "type": "string", "description": "Display title rendered inside .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional short hint rendered inside .hd > .hint." },
    "varieties": {
      "type": "array",
      "minItems": 1,
      "description": "Case library shown in the dropdown.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "h"],
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$", "description": "Stable option value." },
          "label": { "type": "string", "description": "Plain-text dropdown label (no LaTeX)." },
          "h": {
            "type": "array",
            "minItems": 1,
            "description": "Square Hodge matrix h[p][q] = dim H^q(X, Omega^p). Complex dimension d derived as h.length-1.",
            "items": { "type": "array", "minItems": 1, "items": { "type": "integer", "minimum": 0 } }
          }
        }
      }
    }
  }
}
