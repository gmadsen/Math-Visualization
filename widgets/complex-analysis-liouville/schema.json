{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/complex-analysis-liouville/schema.json",
  "title": "complex-analysis-liouville widget params",
  "description": "Liouville's-theorem / Cauchy-estimate explorer. The reader picks an entire function f and a radius R; the widget computes M_R = max_{|z|=R}|f| numerically and plots the Cauchy estimate bound M_R/R against R, together with the actual |f'(0)|. The point: |f'(0)| ≤ M_R/R for every R, so if f is BOUNDED (M_R ≤ M) the bound → 0 as R → ∞, forcing f' = 0 and hence f constant — Liouville's theorem. For genuinely entire non-constant f (z, 2z+1, z², e^z) the bound is finite or diverges, never collapsing to 0. The function eval is intrinsic (a `kind` enum); params carry the case menu plus each f's |f'(0)|.",
  "meta": {
    "family": "complex-analysis-liouville",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title", "functions"],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id; the script derives -sel/-r/-svg/-out." },
    "title": { "type": "string", "description": "Display title rendered inside .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional short hint rendered inside .hd > .hint." },
    "functions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 6,
      "description": "Each entire function offered in the dropdown.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "kind", "fp0"],
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$", "description": "Stable option value." },
          "label": { "type": "string", "description": "Plain-text dropdown label, e.g. 'e^z'." },
          "kind": {
            "type": "string",
            "enum": ["const", "z", "affine", "z2", "ez"],
            "description": "Which f to evaluate. const = 2; affine = 2z+1."
          },
          "fp0": { "type": "number", "minimum": 0, "description": "The actual |f'(0)|, drawn as the reference line the Cauchy bound must dominate." },
          "note": { "type": "string", "maxLength": 110, "description": "Optional plain-text remark about this case." }
        }
      }
    }
  }
}
