// cmb-w3 widget — bespoke verbatim slug for one widget on the
// computational-molecular-biology topic. Uses the shared verbatim renderer
// (widgets/_shared/verbatim-renderer.mjs) so the markup and driving script
// are carried as opaque strings in params.

export { renderMarkup, renderScript } from '../_shared/verbatim-renderer.mjs';
