{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/hodge-theory-hodge-diamond/schema.json",
  "title": "hodge-theory-hodge-diamond widget params",
  "description": "Interactive Hodge-diamond explorer. The reader picks a variety from a dropdown and its Hodge numbers h^{p,q} fill a rotated diamond grid; clicking an entry highlights its Hodge-conjugate (q,p) and Poincaré-dual (d-p,d-q) partners, and the readout reports the Betti numbers (row sums), total dimension, Euler characteristic, and a live check of the two symmetry laws. Fully data-driven: each variety carries its complex dimension and a (d+1)x(d+1) matrix of Hodge numbers, so a non-HTML frontend can rebuild the diamond from params alone.",
  "meta": {
    "family": "hodge-theory-hodge-diamond",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "title",
    "varieties"
  ],
  "properties": {
    "widgetId": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "DOM id for the outer <div class=\"widget\"> wrapper."
    },
    "title": {
      "type": "string",
      "description": "Display title rendered inside .hd > .ttl."
    },
    "hint": {
      "type": "string",
      "description": "Optional short hint rendered inside .hd > .hint."
    },
    "varieties": {
      "type": "array",
      "minItems": 1,
      "description": "The case library shown in the dropdown. Each entry is one variety with its Hodge numbers.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "h"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[A-Za-z0-9_-]+$",
            "description": "Stable option value (no spaces)."
          },
          "label": {
            "type": "string",
            "description": "Plain-text dropdown label (no LaTeX — native <option> renders as plain text)."
          },
          "h": {
            "type": "array",
            "minItems": 1,
            "description": "Hodge numbers as a square matrix h[p][q] = dim H^q(X, Omega^p), row index p, column index q. The complex dimension d is derived as h.length - 1 (no separate d field, so the two cannot disagree); the matrix is expected square, each row having h.length entries.",
            "items": {
              "type": "array",
              "minItems": 1,
              "items": { "type": "integer", "minimum": 0 }
            }
          },
          "note": {
            "type": "string",
            "description": "Optional one-line fact shown in the readout when this variety is selected."
          }
        }
      }
    }
  }
}
