{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/algebraic-de-rham-hodge-sandbox/schema.json",
  "title": "algebraic-de-rham-hodge-sandbox widget params",
  "description": "Sandbox for assembling Hodge diamonds from genus + dimension data, used in algebraic-de-rham-cohomology.html §6.",
  "meta": {
    "family": "algebraic-de-rham-hodge-sandbox",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "bodyMarkup",
    "bodyScript"
  ],
  "properties": {
    "widgetId": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "DOM id for the widget host element. Must match the id baked into bodyMarkup."
    },
    "bodyMarkup": {
      "type": "string",
      "description": "ARTIFACT. Verbatim widget host bytes (the inline `<div class=\"widget\" id=\"...\"></div>`). Carried through unchanged so the round-trip stays byte-identical."
    },
    "bodyScript": {
      "type": "string",
      "description": "ARTIFACT. Verbatim driving script bytes including the leading newline + `<script>` tag and trailing `</script>`. Carries the IIFE that wires up the widget; preserved byte-for-byte."
    }
  }
}
