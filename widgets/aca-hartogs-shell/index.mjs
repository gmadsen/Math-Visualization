// aca-hartogs-shell widget — bespoke registry entry for advanced-complex-analysis.
// Renderer shape (markup + IIFE script) is shared with the rest of the aca-* family.

export { renderMarkup, renderScript } from '../_shared/aca-figure-renderer.mjs';
