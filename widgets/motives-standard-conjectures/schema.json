{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/motives-standard-conjectures/schema.json",
  "title": "motives-standard-conjectures widget params",
  "description": "Status browser for Grothendieck's standard conjectures and the unconditional results around them. The reader clicks an item (Lefschetz B, Künneth C, Hodge standard I, num=hom D, plus theorems like Jannsen semisimplicity, mixed Tate motives, Voevodsky's DM) and the readout gives its statement, status (known / partial / open), and what is known. Data-driven: each item carries its status and explanation in params.",
  "meta": {
    "family": "motives-standard-conjectures",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title", "items"],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id; the script derives -svg/-out." },
    "title": { "type": "string", "description": "Display title rendered inside .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional short hint rendered inside .hd > .hint." },
    "items": {
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "description": "The conjectures / results as clickable rows, top to bottom.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "name", "statement", "status", "reason"],
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$", "description": "Stable row id." },
          "name": { "type": "string", "maxLength": 32, "description": "Short name, e.g. 'Lefschetz (B)'." },
          "statement": { "type": "string", "maxLength": 72, "description": "One-line statement shown on the row." },
          "status": {
            "type": "string",
            "enum": ["known", "partial", "open"],
            "description": "known (proven unconditionally) / partial (known in special cases) / open."
          },
          "reason": { "type": "string", "description": "What is known and why — shown in the readout on click." }
        }
      }
    }
  }
}
