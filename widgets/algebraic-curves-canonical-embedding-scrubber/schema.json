{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/algebraic-curves-canonical-embedding-scrubber/schema.json",
  "title": "algebraic-curves-canonical-embedding-scrubber widget params",
  "description": "Proof-scrubber walkthrough of the canonical embedding $C \\hookrightarrow \\mathbb{P}^{g-1}$. Used in algebraic-curves-higher-genus.html §4.",
  "meta": {
    "family": "algebraic-curves-canonical-embedding-scrubber",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "bodyMarkup",
    "bodyScript"
  ],
  "properties": {
    "widgetId": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "DOM id for the widget host element. Must match the id baked into bodyMarkup."
    },
    "bodyMarkup": {
      "type": "string",
      "description": "ARTIFACT. Verbatim widget host bytes (the inline `<div class=\"widget\" id=\"...\"></div>`). Carried through unchanged so the round-trip stays byte-identical."
    },
    "bodyScript": {
      "type": "string",
      "description": "ARTIFACT. Verbatim driving script bytes including the leading newline + `<script>` tag and trailing `</script>`. Carries the IIFE that wires up the widget; preserved byte-for-byte."
    }
  }
}
