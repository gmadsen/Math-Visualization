{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/weight-ladder-sl2/schema.json",
  "title": "weight-ladder-sl2 widget params",
  "description": "A self-contained 'ladder-op' engine for the irreducible sl_2(C) representations V_n. The reader sets the highest weight n (so dim V_n = n+1) and applies the raising operator e and lowering operator f to walk a basis vector up and down the weight ladder n, n-2, ..., -n: f lowers the weight by 2 (f.v_k = (k+1) v_{k+1}, killed at the bottom v_n), e raises by 2 (e.v_k = (n-k+1) v_{k-1}, killed at the top v_0), with the integer action coefficients shown. Starting from the highest-weight vector and dropping f repeatedly spawns the whole (n+1)-dimensional irrep — the sl_2 rule that classifies everything. The renderer owns the weight ladder, the current-vector highlight, the e/f action with coefficients and the kill-at-the-ends behaviour, the dimension control, and the readout; the representation is the fixed V_n family. The gesture is 'ladder-op' — applying raising/lowering operators and watching a weight string fill out. jsdom-safe (no getScreenCTM/rAF; button-driven).",
  "meta": {
    "family": "weight-ladder-sl2",
    "dimension": "2d",
    "gesture": "ladder-op",
    "role": "explore"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "svgId",
    "outputId",
    "title"
  ],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id for the outer <div class=\"widget\"> wrapper. The e / f / reset / dim buttons derive their ids from it." },
    "svgId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id for the <svg> drawing surface." },
    "outputId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id for the .readout (current weight, e/f action)." },
    "title": { "type": "string", "description": "Display title in .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional hint in .hd > .hint. Should tell the reader to apply e (raise) and f (lower)." },
    "svgTitle": { "type": "string", "description": "Accessible <title> inside the SVG. Defaults to title." },
    "viewBox": { "type": "string", "description": "SVG viewBox. Defaults to '0 0 460 420'.", "default": "0 0 460 420" },
    "svgWidth": { "type": "integer", "description": "SVG width attr. Defaults to 460.", "default": 460 },
    "svgHeight": { "type": "integer", "description": "SVG height attr. Defaults to 420.", "default": 420 },
    "nInit": { "type": "integer", "description": "Initial highest weight n (dimension n+1). Default 4.", "minimum": 1, "default": 4 },
    "nMax": { "type": "integer", "description": "Largest highest weight n the dimension control allows. Default 7 (so up to dim 8).", "minimum": 1, "default": 7 }
  }
}
