{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/ec-disc/schema.json",
  "title": "ec-disc widget params",
  "description": "The \"Discriminant watch\" widget on the elliptic-curves topic, migrated from a verbatim (opaque bodyMarkup/bodyScript) slug to a semantic renderer. Two sliders a, b drive the discriminant of y^2 = x^3 + a x + b, Delta = -16(4a^3 + 27 b^2); the readout reports the value and classifies the real locus (Delta>0: oval + unbounded component; Delta<0: one unbounded component; Delta~0: node/cusp). Slider ranges, defaults, the header title/hint, and the DOM id prefix are now inspectable params (AJV-validated) rather than buried in an opaque markup string.",
  "meta": {
    "family": "ec-disc",
    "dimension": "2d",
    "gesture": "slider",
    "role": "interactive"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "title",
    "hint",
    "idPrefix",
    "a",
    "b"
  ],
  "properties": {
    "widgetId": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "DOM id for the widget root div (the migration adds the host id the original inline markup lacked)."
    },
    "title": {
      "type": "string",
      "description": "Header title rendered in .hd > .ttl."
    },
    "hint": {
      "type": "string",
      "description": "Header hint rendered in .hd > .hint."
    },
    "idPrefix": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "Prefix for the widget's DOM ids (e.g. 'disc' -> disc-a, disc-b, disc-out)."
    },
    "a": { "$ref": "#/$defs/axis" },
    "b": { "$ref": "#/$defs/axis" }
  },
  "$defs": {
    "axis": {
      "type": "object",
      "additionalProperties": false,
      "required": ["min", "max", "step", "value"],
      "properties": {
        "min": { "type": "number" },
        "max": { "type": "number" },
        "step": { "type": "number", "exclusiveMinimum": 0 },
        "value": { "type": "number" }
      },
      "description": "Range-slider configuration for one Weierstrass coefficient."
    }
  }
}
