// cm-lte widget — bespoke registry entry for the condensed-mathematics topic.
// Uses the shared verbatim renderer (widgets/_shared/verbatim-renderer.mjs) so
// byte-identical round-trip is preserved.

export { renderMarkup, renderScript } from '../_shared/verbatim-renderer.mjs';
