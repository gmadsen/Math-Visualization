{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/motives-chow-decomposition/schema.json",
  "title": "motives-chow-decomposition widget params",
  "description": "Chow-motive decomposition viewer. The reader picks a variety and sees its motive h(X) split into pieces — Tate motives 𝟙 = ℚ(0), 𝕃 = ℚ(−1), 𝕃², … (each 1-dimensional, pure type (i,i)) plus the 'interesting' h^1 pieces of curves. Each piece shows which cohomological degree it realizes into and its dimension, and the readout recovers the Betti numbers. Data-driven: each variety carries its list of motive pieces in params.",
  "meta": {
    "family": "motives-chow-decomposition",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title", "varieties"],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id; the script derives -sel/-svg/-out." },
    "title": { "type": "string", "description": "Display title rendered inside .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional short hint rendered inside .hd > .hint." },
    "varieties": {
      "type": "array",
      "minItems": 1,
      "description": "Case library shown in the dropdown.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "pieces"],
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$", "description": "Stable option value." },
          "label": { "type": "string", "description": "Plain-text dropdown label (no LaTeX)." },
          "note": { "type": "string", "description": "Optional one-line decomposition fact shown in the readout." },
          "pieces": {
            "type": "array",
            "minItems": 1,
            "maxItems": 10,
            "description": "The summands of h(X), in increasing realization degree.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["label", "degree", "dim", "kind"],
              "properties": {
                "label": { "type": "string", "maxLength": 16, "description": "Short plain-text name, e.g. '𝟙', '𝕃', '𝕃²', 'h¹(C)'." },
                "degree": { "type": "integer", "minimum": 0, "description": "Cohomological degree H^degree this piece realizes into." },
                "dim": { "type": "integer", "minimum": 1, "description": "Dimension of one copy of this piece." },
                "mult": { "type": "integer", "minimum": 1, "default": 1, "description": "Multiplicity (number of copies in the decomposition)." },
                "kind": { "type": "string", "enum": ["tate", "transcendental"], "description": "tate = a Tate motive 𝕃^i (type (i,i)); transcendental = an h^1-type piece." }
              }
            }
          }
        }
      }
    }
  }
}
