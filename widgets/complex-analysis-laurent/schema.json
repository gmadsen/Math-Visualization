{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/complex-analysis-laurent/schema.json",
  "title": "complex-analysis-laurent widget params",
  "description": "Laurent-series annulus explorer. The reader picks a (function, annulus) case — e.g. 1/(z(z−1)) has DIFFERENT Laurent series on 0<|z|<1 and on |z|>1 — and a number of terms N; the widget shades the annulus, places a test point in it, and shows the partial Laurent sum converging to f there (and the series formula, regular + principal parts). The function eval + partial sums are intrinsic (a `kind`/`seriesKind` enum); params carry the menu + series text.",
  "meta": {
    "family": "complex-analysis-laurent",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title", "cases"],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id; the script derives -sel/-n/-svg/-out." },
    "title": { "type": "string", "description": "Display title rendered inside .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional short hint rendered inside .hd > .hint." },
    "cases": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "description": "Each (function, annulus) combination offered in the dropdown.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "kind", "seriesKind", "rIn", "rOut", "series"],
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$", "description": "Stable option value." },
          "label": { "type": "string", "description": "Plain-text dropdown label." },
          "kind": { "type": "string", "enum": ["inv_zzm1", "exp_invz"], "description": "Function to evaluate directly." },
          "seriesKind": { "type": "string", "enum": ["inner_zzm1", "outer_zzm1", "exp_invz"], "description": "Which Laurent partial-sum rule to use." },
          "rIn": { "type": "number", "minimum": 0, "description": "Inner radius of the annulus." },
          "rOut": { "type": "number", "exclusiveMinimum": 0, "description": "Outer radius for display (use a finite value for |z|>r annuli)." },
          "series": { "type": "string", "maxLength": 60, "description": "Plain-text Laurent series for this annulus." }
        }
      }
    }
  }
}
