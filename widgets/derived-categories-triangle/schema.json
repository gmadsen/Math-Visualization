{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/derived-categories-triangle/schema.json",
  "title": "derived-categories-triangle widget params",
  "description": "Bespoke widget for the distinguished triangle axioms (TR1)–(TR4) in a triangulated category, used in derived-categories.html §4 (#triangulated-structure).",
  "meta": {
    "family": "clickable-diagram",
    "dimension": "2d",
    "gesture": "click",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "bodyMarkup",
    "bodyScript"
  ],
  "properties": {
    "widgetId": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "DOM id of the widget host element. Must match the id baked into bodyMarkup."
    },
    "bodyMarkup": {
      "type": "string",
      "description": "ARTIFACT. Verbatim widget host bytes — the inline `<div class=\"widget\" id=\"...\">…</div>`. Carried through unchanged so the round-trip stays byte-identical."
    },
    "bodyScript": {
      "type": "string",
      "description": "ARTIFACT. Verbatim driving script bytes including the leading newline + `<script>` tag and trailing `</script>`. Carries the IIFE that wires up the widget; preserved byte-for-byte."
    }
  }
}
