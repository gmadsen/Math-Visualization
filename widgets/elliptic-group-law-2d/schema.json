{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/elliptic-group-law-2d/schema.json",
  "title": "elliptic-group-law-2d widget params",
  "description": "A self-contained 'feel the group law' engine for a real elliptic curve y^2 = x^3 + a x + b. The reader drags two points P and Q constrained to the curve; the engine draws the chord through them (or the tangent when P = Q), finds its third intersection R with the cubic, reflects R across the x-axis to give P + Q, and reports the coordinates of P, Q and P+Q. Vertical chords (Q = -P) give the point at infinity O (the identity). The renderer owns the curve sampling, the on-curve drag (pointer x -> nearest curve point on the chosen branch), the chord/tangent construction, the group-law arithmetic, and Reset; the curve is param-driven (a, b, initial point x-coordinates). The gesture is 'drag-on-curve' — direct manipulation of points pinned to a curve, a dimension the corpus otherwise lacks. jsdom-safe: createSVGPoint/getScreenCTM run only inside the pointer handlers, never at init.",
  "meta": {
    "family": "elliptic-group-law-2d",
    "dimension": "2d",
    "gesture": "drag-on-curve",
    "role": "explore"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "svgId",
    "outputId",
    "title"
  ],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id for the outer <div class=\"widget\"> wrapper. The Reset button derives its id from it (widgetId+'-reset')." },
    "svgId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id for the <svg> drawing surface." },
    "outputId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id for the .readout the engine writes P, Q, P+Q into." },
    "title": { "type": "string", "description": "Display title in .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional hint in .hd > .hint. Should tell the reader to drag P and Q along the curve." },
    "svgTitle": { "type": "string", "description": "Accessible <title> inside the SVG. Defaults to title." },
    "viewBox": { "type": "string", "description": "SVG viewBox. Defaults to '0 0 600 460'.", "default": "0 0 600 460" },
    "svgWidth": { "type": "integer", "description": "SVG width attr. Defaults to 600.", "default": 600 },
    "svgHeight": { "type": "integer", "description": "SVG height attr. Defaults to 460.", "default": 460 },
    "a": { "type": "number", "description": "Coefficient a in y^2 = x^3 + a x + b. Default -1.", "default": -1 },
    "b": { "type": "number", "description": "Coefficient b in y^2 = x^3 + a x + b. Default 1.", "default": 1 },
    "x0": { "type": "number", "description": "Data x at the left edge. Default -2.2.", "default": -2.2 },
    "x1": { "type": "number", "description": "Data x at the right edge. Default 3.", "default": 3 },
    "y0": { "type": "number", "description": "Data y at the bottom edge. Default -3.6.", "default": -3.6 },
    "y1": { "type": "number", "description": "Data y at the top edge. Default 3.6.", "default": 3.6 },
    "initialP": { "type": "number", "description": "Initial x-coordinate of P (placed on the upper branch). Default -0.6.", "default": -0.6 },
    "initialQ": { "type": "number", "description": "Initial x-coordinate of Q (placed on the upper branch). Default 1.4.", "default": 1.4 },
    "resetLabel": { "type": "string", "description": "Reset control label. Default '↺ Reset'.", "default": "↺ Reset" }
  }
}
