{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/complex-analysis-monodromy/schema.json",
  "title": "complex-analysis-monodromy widget params",
  "description": "Monodromy visualizer for multivalued functions. The reader picks a function (√z, z^{1/3}, log z) and sweeps the angle of a point travelling around the branch point 0; the analytically continued value is traced in the w-plane. After one full loop √z returns negated, z^{1/3} rotated by 120°, and log z shifted by 2πi — the monodromy. The function continuation is intrinsic; params carry the menu + the monodromy text.",
  "meta": {
    "family": "complex-analysis-monodromy",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title", "functions"],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id; the script derives -sel/-ang/-svg/-out." },
    "title": { "type": "string", "description": "Display title rendered inside .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional short hint rendered inside .hd > .hint." },
    "functions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 6,
      "description": "Multivalued functions offered in the dropdown.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "kind", "monodromy"],
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$", "description": "Stable option value." },
          "label": { "type": "string", "description": "Plain-text dropdown label, e.g. '√z'." },
          "kind": {
            "type": "string",
            "enum": ["sqrt", "cbrt", "log"],
            "description": "Which continuation: √z, z^{1/3}, or log z."
          },
          "monodromy": { "type": "string", "maxLength": 80, "description": "Plain-text statement of the per-loop monodromy." }
        }
      }
    }
  }
}
