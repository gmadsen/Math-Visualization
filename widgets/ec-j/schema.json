{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/ec-j/schema.json",
  "title": "ec-j widget params",
  "description": "The j-invariant calculator on the elliptic-curves topic, migrated from a verbatim slug to a semantic renderer. Two sliders a, b drive j = 1728 * 4a^3 / (4a^3 + 27 b^2) for y^2 = x^3 + a x + b, with the discriminant and CM annotations (j=0: CM by Z[omega]; j=1728: CM by Z[i]). A gallery of preset buttons jumps to famous complex-multiplication curves. Slider ranges/defaults, header title/hint, the DOM id prefix, and the preset list (a, b, label) are now inspectable, AJV-validated params.",
  "meta": {
    "family": "ec-j",
    "dimension": "2d",
    "gesture": "slider",
    "role": "interactive"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title", "hint", "idPrefix", "a", "b", "presets"],
  "properties": {
    "widgetId": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "DOM id for the widget root div."
    },
    "title": { "type": "string", "description": "Header title (.hd > .ttl)." },
    "hint": { "type": "string", "description": "Header hint (.hd > .hint)." },
    "idPrefix": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "Prefix for the widget's DOM ids (e.g. 'j' -> j-a, j-b, j-out)."
    },
    "a": { "$ref": "#/$defs/axis" },
    "b": { "$ref": "#/$defs/axis" },
    "presets": {
      "type": "array",
      "description": "Famous complex-multiplication curves to jump to, as buttons.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["a", "b", "label"],
        "properties": {
          "a": { "type": "number", "description": "Weierstrass coefficient a." },
          "b": { "type": "number", "description": "Weierstrass coefficient b." },
          "label": { "type": "string", "description": "Button label (may contain KaTeX)." }
        }
      }
    }
  },
  "$defs": {
    "axis": {
      "type": "object",
      "additionalProperties": false,
      "required": ["min", "max", "step", "value"],
      "properties": {
        "min": { "type": "number" },
        "max": { "type": "number" },
        "step": { "type": "number", "exclusiveMinimum": 0 },
        "value": { "type": "number" }
      },
      "description": "Range-slider configuration for one Weierstrass coefficient."
    }
  }
}
