{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/hodge-theory-period-elliptic/schema.json",
  "title": "hodge-theory-period-elliptic widget params",
  "description": "Period map for elliptic curves: the weight-1 period domain is the upper half-plane ℍ (= the Siegel space H_1). The reader moves a period τ in ℍ with Re/Im sliders (or jumps to a preset lattice); the widget shades the SL₂(ℤ) fundamental domain, reduces τ into it (the period map modulo monodromy), and computes the modular j-invariant — recovering the j-line. The computation (SL₂(ℤ) reduction, j q-series) is intrinsic; params only carry optional notable presets.",
  "meta": {
    "family": "hodge-theory-period-elliptic",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title"],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id; the script derives -re/-im/-preset/-svg/-out." },
    "title": { "type": "string", "description": "Display title rendered inside .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional short hint rendered inside .hd > .hint." },
    "presets": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "description": "Notable period points the reader can jump to from a dropdown.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "re", "im"],
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$", "description": "Stable option value." },
          "label": { "type": "string", "description": "Plain-text dropdown label (no LaTeX)." },
          "re": { "type": "number", "description": "Re(τ)." },
          "im": { "type": "number", "exclusiveMinimum": 0, "description": "Im(τ) (must be positive — τ lies in the upper half-plane)." }
        }
      }
    }
  }
}
