{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/complex-multiplication-w6/schema.json",
  "title": "complex-multiplication-w6 widget params",
  "description": "Verbatim-renderer slug for an inline widget on the complex-multiplication topic.",
  "meta": {
    "family": "verbatim",
    "dimension": "2d",
    "gesture": "select",
    "role": "interactive"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "title",
    "hint",
    "bodyMarkup",
    "bodyScript"
  ],
  "properties": {
    "widgetId": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "DOM id."
    },
    "title": {
      "type": "string",
      "description": "Header title."
    },
    "hint": {
      "type": "string",
      "description": "Header hint."
    },
    "bodyMarkup": {
      "type": "string",
      "minLength": 1,
      "description": "Verbatim markup.",
      "x-artifact": true
    },
    "bodyScript": {
      "type": "string",
      "description": "Verbatim driving script.",
      "x-artifact": true
    }
  }
}
