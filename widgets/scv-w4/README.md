# scv-w4

Verbatim-renderer slug for one inline widget on `several-complex-variables.html`. Body markup and script are stored opaquely in `params.bodyMarkup`/`params.bodyScript`; future migration could hoist semantic params (slider ranges, color tokens) out of the strings.
