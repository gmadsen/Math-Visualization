// sg-gen widget — bespoke verbatim slug for the semigroup-theory-evolution-equations topic.
// Uses the shared verbatim renderer (widgets/_shared/verbatim-renderer.mjs) so byte-identical
// round-trip is preserved while satisfying the registry-driven audit gate.

export { renderMarkup, renderScript } from '../_shared/verbatim-renderer.mjs';
