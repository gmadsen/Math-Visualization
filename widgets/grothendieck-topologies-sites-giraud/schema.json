{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/grothendieck-topologies-sites-giraud/schema.json",
  "title": "grothendieck-topologies-sites-giraud widget params",
  "description": "Bespoke registry entry for a script-less widget. Carries the widget's HTML body verbatim as an artifact string so the byte-identical round-trip gate passes. The topic JSON does not emit a paired widget-script block. A portable React/Three.js consumer would discard the artifact and re-implement the widget in its own renderer.",
  "meta": {
    "family": "declarative-host",
    "dimension": "2d",
    "gesture": "click",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "bodyMarkup"
  ],
  "properties": {
    "widgetId": {
      "type": "string",
      "pattern": "^[A-Za-z][A-Za-z0-9_-]*$",
      "description": "DOM id of the widget's outer wrapper. Mirrored inside bodyMarkup; carried separately for auditing and alternate-frontend portability."
    },
    "bodyMarkup": {
      "type": "string",
      "description": "ARTIFACT. Verbatim widget HTML — the entire <div class=\"widget\" id=\"…\">…</div> bytes. Preserved byte-for-byte."
    }
  }
}
