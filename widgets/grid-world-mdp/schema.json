{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/grid-world-mdp/schema.json",
  "title": "grid-world-mdp widget params",
  "description": "A self-contained 'edit-grid' widget for a grid-world Markov decision process. The states are the cells of a rows x cols grid: empty cells (a step reward), goal cells (positive terminal reward), pit cells (negative terminal reward), and walls (blocked). Actions are the four moves; transitions are stochastic — the intended move succeeds with probability 1 - noise and slips to each perpendicular direction with probability noise/2 (a wall or edge bounces back). The widget solves the Bellman optimality equation V*(s) = max_a { R + gamma sum_s' P(s'|s,a) V*(s') } by value iteration to convergence and PAINTS the optimal value V* (cell shading + number) and the greedy policy pi* (an arrow per non-terminal cell). The reader CLICKS a cell to cycle its type (empty -> goal -> pit -> wall -> empty) and adjusts the discount gamma and the action noise; the value function and policy re-solve live, so e.g. raising the noise makes the policy route cautiously away from the pit. Pure DOM/SVG; jsdom-safe (click-driven; no getScreenCTM/rAF).",
  "meta": {
    "family": "grid-world-mdp",
    "dimension": "2d",
    "gesture": "edit-grid",
    "role": "explore"
  },
  "type": "object",
  "additionalProperties": false,
  "required": [
    "widgetId",
    "svgId",
    "outputId",
    "title",
    "layout"
  ],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id for the outer <div class=\"widget\"> wrapper. The gamma/noise/reset buttons derive their ids from it." },
    "svgId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id for the <svg> drawing the grid, the values, and the policy arrows." },
    "outputId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id for the .readout (gamma, noise, iterations to converge, hints)." },
    "title": { "type": "string", "description": "Display title in .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional hint in .hd > .hint. Should tell the reader to click a cell to change its type and adjust gamma / noise." },
    "svgTitle": { "type": "string", "description": "Accessible <title> inside the SVG. Defaults to title." },
    "layout": {
      "type": "array",
      "description": "The grid, one string per row; characters: '.' empty, 'G' goal, 'P' pit, '#' wall. All rows must have equal length. E.g. the classic 3x4 world: ['...G', '.#.P', '....'].",
      "minItems": 1,
      "items": { "type": "string", "pattern": "^[.GP#]+$" }
    },
    "goalReward": { "type": "number", "description": "Terminal reward at a goal cell. Default 1.", "default": 1 },
    "pitReward": { "type": "number", "description": "Terminal reward at a pit cell. Default -1.", "default": -1 },
    "stepReward": { "type": "number", "description": "Reward for each move from a non-terminal cell (the living cost). Default -0.04.", "default": -0.04 },
    "gammaInit": { "type": "number", "minimum": 0, "maximum": 1, "description": "Initial discount factor gamma. Default 1.", "default": 1 },
    "noiseInit": { "type": "number", "minimum": 0, "maximum": 1, "description": "Initial action noise (slip probability total). Default 0.2.", "default": 0.2 },
    "viewBox": { "type": "string", "description": "SVG viewBox. Default derived from grid if omitted." },
    "svgWidth": { "type": "integer", "description": "SVG width attr." },
    "svgHeight": { "type": "integer", "description": "SVG height attr." }
  }
}
