{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://math-vis.local/widgets/complex-analysis-max-modulus/schema.json",
  "title": "complex-analysis-max-modulus widget params",
  "description": "Maximum-modulus-principle heatmap. The reader picks a holomorphic f; the widget shades the closed unit disk by |f(z)| (brighter = larger) and marks where |f| is largest — always on the boundary. The readout compares the boundary maximum to the largest interior value, confirming a non-constant holomorphic function has no interior local maximum of |f|. The function evaluation is intrinsic (a `kind` enum); params carry the menu.",
  "meta": {
    "family": "complex-analysis-max-modulus",
    "dimension": "2d",
    "gesture": "interact",
    "role": "exploratory"
  },
  "type": "object",
  "additionalProperties": false,
  "required": ["widgetId", "title", "functions"],
  "properties": {
    "widgetId": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]*$", "description": "DOM id; the script derives -sel/-svg/-out." },
    "title": { "type": "string", "description": "Display title rendered inside .hd > .ttl." },
    "hint": { "type": "string", "description": "Optional short hint rendered inside .hd > .hint." },
    "functions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "description": "Holomorphic functions offered in the dropdown.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "kind"],
        "properties": {
          "id": { "type": "string", "pattern": "^[A-Za-z0-9_-]+$", "description": "Stable option value." },
          "label": { "type": "string", "description": "Plain-text dropdown label, e.g. 'e^z'." },
          "kind": {
            "type": "string",
            "enum": ["sq", "exp", "cube_m_z", "sinz", "poly211"],
            "description": "Which function: z², e^z, z³−z, sin z, or z²+z+1."
          }
        }
      }
    }
  }
}
