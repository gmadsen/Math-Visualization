# oc-reach

Reachable-set widget for the double-integrator under bounded control.

Verbatim-renderer slug. Params: `widgetId`, `title`, `hint`, `bodyMarkup` (full widget HTML), `bodyScript` (driving script).
