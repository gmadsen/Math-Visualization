# OS / editor junk
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Node (if you ever run a local server with npm)
node_modules/
npm-debug.log
package-lock.json
# CI needs the scripts lockfile for `npm ci --prefix scripts` — keep it tracked
!scripts/package-lock.json

# Local verification and scratch scripts — not part of the notebook
_verify*.js
_sidetoc_verify.js
test_index.js

# Playwright/Chrome MCP scratch output
.playwright-mcp/

# Screenshot scratch from agent browser-MCP verification runs.
# These land in repo root from Playwright/Chrome DevTools MCP calls.
/de-rham-*.png
/page-*.png

.claude/worktrees
.claude/worktree
.claude/settings.local.json

# examples/ build outputs
examples/react-consumer/render.html
